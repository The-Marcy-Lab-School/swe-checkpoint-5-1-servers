# swe-casestudy-5-bookmark-manager

To run the case study, use these commands:

```sh
# Install Dependencies (Express, Nodemon)
npm i

# Start the Server
npm start
```

The server will be running at [http://localhost:8080/](http://localhost:8080/)